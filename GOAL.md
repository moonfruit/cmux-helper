# alfred cmux-helper workflow

## ssh

1. 打开 Alfred 输入 "ssh" 来触发 cmux-helper workflow
2. 能够快速选择和连接到预设的 SSH 主机
3. 可选择使用 `cmux ssh user@host` 命令或 `cmux send 'ssh user@host\n'` 进行连接
4. 使用 `cmux ssh user@host` 时，应将其所在的窗口移到前台
5. ssh 主机来源之一是 ~/.ssh/saved_hosts
6. 可以为对应的 ssh 主机设置别名/标签，方便快速连接
